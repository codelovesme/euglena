| The nucleus boots the cell: one handler, fired exactly once at birth.

gene_name = "nucleus"

EuglenaHasBeenBorn { cell_name } =>
    emit Log { source = "nucleus", level = "Info", message = "[$cell_name] cell booted" } to logger
    return Alive { cell_name = cell_name }
