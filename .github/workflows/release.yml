name: Release

on:
  push:
    tags: ["v*"]
  # Manual dry-run: builds, sanity-checks, and uploads an Actions artifact but
  # does NOT create a GitHub Release (guarded by `if: github.ref_type == 'tag'`
  # below), so packaging can be exercised without cutting a real tag.
  workflow_dispatch: {}

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write

jobs:
  build-release:
    name: Build release tarball (Linux x86_64)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Determine version string
        id: version
        run: |
          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            echo "version=${{ github.ref_name }}" >> "$GITHUB_OUTPUT"
          else
            echo "version=dev-${{ github.sha }}" >> "$GITHUB_OUTPUT"
          fi

      # Single source of truth for the shipped version is the git tag: stamp
      # Cargo.toml's [package] version from it before building so the binary's
      # `--version` always matches the release, rather than trusting a
      # hand-edited Cargo.toml. Only the leading-`v`-stripped tag (v0.1.1 ->
      # 0.1.1) is valid semver for Cargo.
      - name: Stamp version from tag
        if: github.ref_type == 'tag'
        run: |
          set -euo pipefail
          v="${{ github.ref_name }}"
          v="${v#v}"
          sed -i "0,/^version = \".*\"/s//version = \"$v\"/" Cargo.toml
          grep -m1 '^version = ' Cargo.toml

      - name: Build release
        run: cargo build --release

      - name: Sanity check — binary runs standalone
        run: |
          set -euo pipefail
          target/release/euglena --version
          target/release/euglena --help >/dev/null

      - name: Package tarball
        run: |
          set -euo pipefail
          v="${{ steps.version.outputs.version }}"
          stage="euglena-${v}-x86_64-linux"
          mkdir -p "$stage"
          cp target/release/euglena "$stage/euglena"
          cp LICENSE README.md "$stage/"
          tar -czf "${stage}.tar.gz" "$stage"
          echo "Built:"
          ls -1 ./*.tar.gz

      # The gene registry. A gene ships as the `.code` source it is — a
      # compiled artifact would be a module, reachable only through an alias,
      # and an alias is the one thing a gene must not have. Source also needs
      # no per-platform matrix: one file serves native, browser and held
      # builds alike.
      #
      # Each gene travels with a small metadata file naming its sha256, which
      # `euglena install` checks before it writes anything and again before
      # it compiles it in.
      - name: Package the genes
        run: |
          set -euo pipefail
          # The tag without its `v`, matching Cargo's version and so the
          # `.code/genes/<name>/<version>/` an install lays down.
          version="${{ steps.version.outputs.version }}"
          version="${version#v}"
          shopt -s nullglob
          for gene in genes/*.gene.code; do
            name="$(basename "$gene" .gene.code)"
            sum="$(sha256sum "$gene" | cut -d' ' -f1)"
            cp "$gene" "${name}.gene.code"
            cat > "${name}.gene.json" <<JSON
          {
            "name": "${name}",
            "version": "${version}",
            "asset": "${name}.gene.code",
            "sha256": "${sum}"
          }
          JSON
            echo "packaged gene ${name}@${version} (sha256 ${sum})"
          done

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: euglena-release-${{ steps.version.outputs.version }}
          path: "*.tar.gz"

      - name: Create GitHub Release
        if: github.ref_type == 'tag'
        uses: softprops/action-gh-release@v2
        with:
          files: |
            *.tar.gz
            *.gene.code
            *.gene.json
          generate_release_notes: true
