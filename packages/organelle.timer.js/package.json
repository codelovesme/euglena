{
    "name": "@euglena/organelle.timer.js",
    "domain": "codeloves.me",
    "version": "1.5.1",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "private": false,
    "license": "GPL v2",
    "author": {
        "name": "Fedai Kaya",
        "email": "codelovesme@gmail.com"
    },
    "scripts": {
        "compile": "tsc -p .",
        "cleanDist": "if [ -d dist ]; then rm -rf dist; fi",
        "cleanDependencies": "if [ -d node_modules ]; then rm -rf dist; fi",
        "clean": "npm run cleanDependencies && npm run cleanDist",
        "test": "npm run compile && jest --testPathPattern=dist/**/*.spec.js"
    },
    "peerDependencies": {
        "@euglena/core": "^1.2.22"
    },
    "dependencies": {
        "cessnalib": "^0.9.0"
    },
    "devDependencies": {
        "@euglena/core": "^1.5.1"
    },
    "files": [
        "dist"
    ]
}
