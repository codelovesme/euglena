{
    "name": "@euglena/organelle.encryptor.jsonwebtoken",
    "domain": "codeloves.me",
    "version": "4.3.0",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "private": false,
    "license": "GPL-2.0",
    "author": {
        "name": "Fedai Kaya",
        "email": "codelovesme@gmail.com"
    },
    "scripts": {
        "compile": "tsc -p .",
        "cleanDist": "if [ -d dist ]; then rm -rf dist; fi",
        "cleanDependencies": "if [ -d node_modules ]; then rm -rf dist; fi",
        "clean": "npm run cleanDependencies && npm run cleanDist",
        "test": "npm run compile && jest --testPathPattern=dist/**/*.spec.js"
    },
    "dependencies": {
        "@types/jsonwebtoken": "^8.3.9",
        "cessnalib": "^0.19.0",
        "jsonwebtoken": "^8.1.0"
    },
    "peerDependencies": {
        "@euglena/core": "^4.3.0",
        "@euglena/template": "^4.3.0"
    },
    "files": [
        "dist"
    ],
    "publishConfig": {
        "access": "public"
    }
}
