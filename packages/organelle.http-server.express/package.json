{
    "name": "@euglena/organelle.http-server.express",
    "domain": "codeloves.me",
    "version": "4.2.1",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "private": false,
    "license": "GPL-2.0",
    "author": {
        "name": "Fedai Kaya",
        "email": "codelovesme@gmail.com"
    },
    "scripts": {
        "compile": "tsc -p .",
        "cleanDist": "if [ -d dist ]; then rm -rf dist; fi",
        "cleanDependencies": "if [ -d node_modules ]; then rm -rf dist; fi",
        "clean": "npm run cleanDependencies && npm run cleanDist",
        "test": "npm run compile && jest --testPathPattern=dist/**/*.spec.js"
    },
    "dependencies": {
        "@types/express": "^4.17.8",
        "cessnalib": "^0.19.0",
        "express": "^4.17.1"
    },
    "peerDependencies": {
        "@euglena/core": "^4.2.1",
        "@euglena/template": "^4.2.1"
    },
    "files": [
        "dist"
    ],
    "publishConfig": {
        "access": "public"
    }
}
