{
    "name": "@euglena/organelle.vacuole.mongodb",
    "domain": "codeloves.me",
    "version": "1.2.27",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "private": false,
    "license": "GPL v2",
    "author": {
        "name": "Fedai Kaya",
        "email": "codelovesme@gmail.com"
    },
    "scripts": {
        "compile": "tsc -p .",
        "cleanDist": "if [ -d dist ]; then rm -rf dist; fi",
        "cleanDependencies":"if [ -d node_modules ]; then rm -rf dist; fi",
        "clean":"npm run cleanDependencies && npm run cleanDist",
        "test": "npm run compile && jest --testPathPattern=dist/**/*.spec.js"
    },
    "peerDependencies": {
        "@euglena/core": "^1.2.22"
    },
    "dependencies": {
        "cessnalib": "^0.9.0",
        "mongodb": "^3.3.2"
    },
    "devDependencies": {
        "@euglena/core": "^1.2.27",
        "@types/mongodb": "~3.3.6"
    },
    "files": [
        "dist"
    ]
}
